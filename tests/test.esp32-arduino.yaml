substitutions:
  name: vitohome-test-arduino
  protocol: P300
  log_frames: "false"
  identify_device: "true"

esp32:
  board: esp32dev
  # See test.esp32-idf.yaml for why this is pinned explicitly.
  toolchain: platformio
  framework:
    type: arduino

<<: !include common.yaml
